import{fireEvent,render,screen,waitFor}from'@testing-library/react';import{beforeEach,expect,test,vi}from'vitest';import App from'./App';beforeEach(()=>{document.cookie='auction_token=; Max-Age=0';vi.stubGlobal('fetch',vi.fn())});test('registers and shows marketplace handoff',async()=>{vi.mocked(fetch).mockResolvedValue(new Response(JSON.stringify({token:'jwt',user:{id:'1',name:'Amina Khan',email:'a@example.com'}}),{status:201,headers:{'Content-Type':'application/json'}}));render(<App/>);fireEvent.click(screen.getByText('Create account'));fireEvent.change(screen.getByLabelText('Full name'),{target:{value:'Amina Khan'}});fireEvent.change(screen.getByLabelText('Email address'),{target:{value:'a@example.com'}});fireEvent.change(screen.getByLabelText('Password'),{target:{value:'secret123'}});fireEvent.click(screen.getAllByRole('button',{name:'Create account'}).at(-1)!);await waitFor(()=>expect(screen.getByText(/Welcome back/)).not.toBeNull())});
